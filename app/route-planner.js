use client'; export default function RoutePlanner({ jobs = [] }) { return <div className="panel"><h2>Route Planner</h2><p>{jobs.length} jobs found.</p>{jobs.map((job, index) => <div className="feature" key={job.id ?? index}><b>Stop {index + 1} - {job.title || 'Job'}</b><span>Customer: {job.customer || 'Not set'}</span><span>Address: {job.address || 'Not set'}</span><span>Engineer: {job.engineer || 'Not assigned'}</span><span>Time: {job.time || 'Not set'}</span></div>)}</div>; }
